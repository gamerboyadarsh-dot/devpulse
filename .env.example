# Copy to `.env` and fill in values as needed.
# Do not commit `.env` — it is gitignored.

# Phase 2: Gemini summarization (not used in Phase 1)
GEMINI_API_KEY=

# dev.to public API base URL (no key required today)
DEV_TO_API_BASE=https://dev.to/api
